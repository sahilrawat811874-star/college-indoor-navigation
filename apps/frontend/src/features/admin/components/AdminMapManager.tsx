import type { CampusMapDto } from '../../../types/map';
export function AdminMapManager({ map }: { map: CampusMapDto }) { return <div className="admin-section"><h2>Map data manager</h2><div className="admin-table"><table><thead><tr><th>Room</th><th>Name</th><th>Block</th><th>Floor</th><th>Type</th><th>Status</th></tr></thead><tbody>{map.rooms.map(r=>{const b=map.blocks.find(x=>x.id===r.blockId); const f=map.floors.find(x=>x.id===r.floorId); return <tr key={r.id}><td>{r.roomNumber}</td><td>{r.name}</td><td>{b?.name}</td><td>{f?.label}</td><td>{r.type}</td><td>{r.isRestricted?'Restricted':'Open'}</td></tr>})}</tbody></table></div></div>; }
